{
  "name": "salsabilla-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run check && npm run build && node scripts/validate-build.mjs",
    "test:browser": "npm run build && node scripts/browser-audit.mjs"
  },
  "engines": {
    "node": ">=22.12.0 <27"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.4",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "playwright": "^1.63.0",
    "typescript": "6.0.3"
  }
}
